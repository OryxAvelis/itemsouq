/**
 * Itemsouq storefront configuration.
 * Add the trusted seller number in international format,
 * digits only (for example: 2126XXXXXXXX). Leave empty to open WhatsApp's
 * contact chooser with a prepared message.
 */
window.ITEMSOUQ_CONFIG = Object.freeze({
  whatsappNumber: ''
});
